"""AI interaction service layer.

The new interaction stack is intentionally backend-owned.  Frontend code should
render the returned state, quick tags, cards and replies instead of making
business decisions itself.
"""

