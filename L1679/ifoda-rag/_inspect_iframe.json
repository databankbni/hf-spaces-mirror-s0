{"function":"() => { const ifr = document.querySelector('iframe#iFrameResizer0'); if (!ifr) return 'NO_IFRAME'; try { const doc = ifr.contentDocument; const win = ifr.contentWindow; if (!doc) return 'NO_DOC'; const out = { url: ifr.src, title: doc.title, bodyText: (doc.body?.innerText||'').slice(0,500), textareas: [], fontBtns: [], rootChildren: doc.getElementById('root')?.children?.length || 0, errors: (win.console?._errors||[]) }; doc.querySelectorAll('textarea').forEach(t => out.textareas.push({ cls: t.className, ph: t.placeholder, rows: t.rows, scrollH: t.scrollHeight, clientH: t.clientHeight, value: (t.value||'').slice(0,40) })); doc.querySelectorAll('button').forEach(b => { const t=(b.textContent||'').trim(); if (t && t.length<=4 && /^\\d$/.test(t)) out.fontBtns.push({ text: t, visible: b.offsetParent !== null }); }); out.allButtons = Array.from(doc.querySelectorAll('button')).slice(0,20).map(b => ({ text: (b.textContent||'').trim().slice(0,40), visible: b.offsetParent !== null })); return JSON.stringify(out, null, 2); } catch (e) { return 'CROSS_ORIGIN: '+e.message; } }"}