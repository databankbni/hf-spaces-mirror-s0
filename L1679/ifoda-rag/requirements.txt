# IFODA RAG — deployment dependencies
# Подобрано под реально существующие версии (не выдуманные пины из исходного
# requirements.txt — те указывают несуществующие torch 2.12.1 / sentence-transformers 5.6.0).
#
# Используется CPU-only torch (≈250MB вместо 2GB+ CUDA-сборки) — для HF Spaces хватает.

--extra-index-url https://download.pytorch.org/whl/cpu

# Core RAG stack
torch>=2.1.0,<3.0
sentence-transformers>=2.7.0,<3.0
transformers>=4.40.0,<5.0
chromadb>=1.0.0,<2.0
scikit-learn>=1.3.0,<2.0
numpy>=1.24.0,<3.0

# Document parsing
pymupdf>=1.24.0,<2.0
python-docx>=1.1.0,<2.0
openpyxl>=3.1.0,<4.0

# LLM + config (optional answer generation)
openai>=1.40.0
python-dotenv>=1.0.0

# API server + Web UI
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0