{"function":"() => { const out = { iframes: [], fonts: [], textareas: [], sizeBtn: null, apiHealth: null }; document.querySelectorAll('iframe').forEach(f => out.iframes.push({ src: f.src, id: f.id, name: f.name, w: f.offsetWidth, h: f.offsetHeight })); document.querySelectorAll('[class*=\"font\" i],[class*=\"size\" i],[aria-label*=\"font\" i],[aria-label*=\"размер\" i],[data-size]').forEach(el => out.fonts.push({ tag: el.tagName, cls: el.className, text: (el.textContent||'').trim().slice(0,80) })); document.querySelectorAll('textarea').forEach(t => { out.textareas.push({ name: t.name, cls: t.className, placeholder: t.placeholder, rows: t.rows, hasOnInput: !!t.oninput, autoResize: t.style.resize || (t.className.match(/auto|resize/i)||[]).join(',') }); }); out.sizeBtn = Array.from(document.querySelectorAll('button')).filter(b => /^[123]?$/.test((b.textContent||'').trim())).map(b => b.textContent.trim()); return JSON.stringify(out, null, 2); }"}