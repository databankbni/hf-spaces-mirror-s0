gradio==6.20.0
duckdb>=1.1
numpy
huggingface_hub>=0.34
# CPU-only torch. The version is pinned to an exact `+cpu` build rather than a
# range: pip gives an extra index no priority, so with a range a later rebuild
# could resolve PyPI's CUDA wheel and drag in gigabytes of unused CUDA deps.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu
sentence-transformers==5.6.1
