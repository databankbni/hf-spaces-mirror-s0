# =========================================================
# requirements.txt — AI Image Hub (CPU-only, HF Spaces free tier)
# Python 3.10 / Docker
# =========================================================

# --- Web server ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# NOTE: torch / torchvision are intentionally NOT listed here.
# They are installed as a separate, earlier step in the Dockerfile using
# the official CPU-only wheel index (torch==2.3.1+cpu / torchvision==
# 0.18.1+cpu). Listing plain "torch==2.3.1" here would make pip re-check
# them against PyPI's default (CUDA) build and risk pulling multi-GB
# NVIDIA wheels that are useless — and slow/failure-prone to fetch — on
# the free CPU Space.

# --- Core scientific / imaging stack ---
numpy==1.26.4
# Pillow is pinned to 9.5.x (NOT 10.x) because simple-lama-inpainting==0.1.2
# hard-requires "pillow<10.0.0,>=9.5.0". This is the only package with an
# upper-bound constraint on Pillow; every other package here (basicsr,
# realesrgan, facexlib, rembg, gfpgan) only requires a lower bound, so
# 9.5.0 satisfies all of them simultaneously.
Pillow==9.5.0
opencv-python-headless==4.10.0.84
scipy==1.13.1

# --- General utilities ---
requests==2.32.3
tqdm==4.66.5
PyYAML==6.0.2
einops==0.8.0
omegaconf==2.3.0
safetensors==0.4.5
huggingface-hub==0.24.6
accelerate==0.33.0

# --- Real-ESRGAN (super-resolution) + BasicSR backbone ---
# Both are real, installable PyPI packages (NOT git-only repos).
basicsr==1.4.2
realesrgan==0.3.0

# --- GFPGAN (face restoration, uses the same basicsr backbone) ---
facexlib==0.3.0
gfpgan==1.3.8

# --- SwinIR-equivalent: served by realesrgan/basicsr arch registry.
#     SwinIR has no official PyPI package (git-only, no proper packaging),
#     so it's provided via its published .pth weights + a small wrapper
#     script instead of a pip dependency. See note in Dockerfile. ---
timm==1.0.8

# --- rembg (background removal) ---
rembg==2.0.59
onnxruntime==1.19.2

# --- LaMa-equivalent: simple-lama-inpainting is a maintained PyPI
#     package that wraps the official LaMa (big-lama) weights with a
#     clean CPU-friendly API (no custom CUDA ops, unlike the original
#     saic-mdal/lama repo). ---
simple-lama-inpainting==0.1.2

# --- kept from the original project (Gradio UI + SDXL inference client) ---
gradio>=4.0.0
jinja2
