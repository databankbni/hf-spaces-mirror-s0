Flask==3.1.3
gunicorn==23.0.0
faster-whisper==1.0.3
requests==2.32.3
# Unpinned: YouTube changes its bot-detection/extraction mechanism often
# enough that yt-dlp's own release cadence (near-weekly) matters more than
# reproducibility here -- always pull latest at build time.
yt-dlp
# yt-dlp plugin that talks to the bgutil-pot sidecar (see start.sh /
# Dockerfile) to fetch PO Tokens over http://127.0.0.1:4416.
bgutil-ytdlp-pot-provider
