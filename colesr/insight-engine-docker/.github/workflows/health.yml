name: Daily Health

on:
  schedule:
    - cron: "0 7 * * *"   # 07:00 UTC daily
  workflow_dispatch:

permissions:
  contents: write

jobs:
  health:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install -r requirements.txt -r requirements-dev.txt

      - name: Collect metrics
        run: python scripts/collect_metrics.py

      - name: Commit metrics
        run: |
          git config user.name "health-bot"
          git config user.email "bot@users.noreply.github.com"
          git add evolution/metrics.json
          git commit -m "metrics: daily health $(date -u +%F)" || echo "no changes"
          git push origin HEAD:main

      - name: Co-evolution dashboard
        if: always()
        run: python scripts/sibling_dashboard.py
